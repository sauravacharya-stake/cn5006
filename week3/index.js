// Declare some functions and variables
function add(a, b) {
  return a + b;
}

const PI = 3.14159;

const subtract = (a, b) => a - b;

// Export the functions and variables so they can be used outside this file
module.exports = {
  add,
  PI,
  subtract,
};

